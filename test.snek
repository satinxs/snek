def fib(n):
    if n < 2:
        return [0,1,1][n] # We return fixed values

    a = 0
    b = 1

    while n > 0:
        t = a
        a = b
        b = b + t
        n -= 1

    return a

print(fib(21))